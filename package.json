{
  "name": "joeri.me",
  "version": "0.0.0",
  "scripts": {
    "dev": "gulp watch & npx @11ty/eleventy --input=src/content --output=public --serve",
    "build": "gulp build && npx @11ty/eleventy  --input=src/content --output=public"
  },
  "devDependencies": {
    "del": "^6.0.0",
    "gulp": "^4.0.2",
    "gulp-sass": "^4.1.0"
  }
}
